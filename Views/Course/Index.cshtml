@model Course

<div class="card mb-3" id=" @Model.Id">
            <div class="row">
                <div class="col md-4">
                    <img src="~/img/@Model.Image" class="img-fluid rounded-start" alt="">
                        </div>
                            <div class="col md-8">
                            <div class="card-body">
                            <h5-title>@Model.Title</h5-title>
                        <p class="card-text">@Model.Description</p>
                    </div>
                </div>
            </div>
       </div> 